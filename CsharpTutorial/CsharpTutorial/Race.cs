//enums has its own file, when found in bigger projects, they are usually stored in a folder called Enums. For example if the system has enums they would be stored in the System.Enums namespace.
//enums are just named values
enum Race {
    earthling,
    alien
    }

